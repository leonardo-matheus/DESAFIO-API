[package]
name = "RUST"
version = "0.1.0"
edition = "2021"

# Dependências para o projeto
[dependencies]
actix-web = "4.0"
rusqlite = { version = "0.26", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cc = "1.0"
